#include<bits/stdc++.h>
using namespace std;
int main()
{
double N,M;
cin>>N>>M;
double result=N/M;
cout<<"floor "<<N<<" / "<<M<<" = " << floor(result)<<endl;
cout<<"ceil "<<N<<" / "<<M<<" = " << ceil(result)<<endl;
cout<<"round "<<N<<" / "<<M<<" = " << round(result)<<endl;
 
}
